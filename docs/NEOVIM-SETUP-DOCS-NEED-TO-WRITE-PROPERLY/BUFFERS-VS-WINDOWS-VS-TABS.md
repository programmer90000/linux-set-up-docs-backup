# Buffers

A buffer is the actual thing you are modifying. For example, a file, a terminal, an unsaved file, a help file, documentation, a file explorer. It is anything which contains modifiable text

# Window

A window is a section which displays a buffer

# Tabs

A tab is a container for windows. It may contain 1 window (like browser tabs) or it can contain multiple windows